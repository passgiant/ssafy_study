T = 10
for test_case in range(1, T+1):
  n = int(input())
  htfs = input()
  sen = input()
  count = 0
  for i, j in enumerate(sen):
    if j == htfs[0]:
      if sen[i:i+len(htfs)] == htfs:
        count += 1
  print(f'#{test_case} {count}')
